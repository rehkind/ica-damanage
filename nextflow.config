manifest {
    name        = 'ica-wgs-delivery'
    description = 'Collect VCF, TSV and CSV outputs in an ICA connector folder'
    version     = '1.2.1'
    nextflowVersion = '>=24.04.0'
}

params {
    input_dir    = ''
    run_name     = ''
    delivery_root = 'production'
    category     = 'wgs'
    outdir       = 'out'
}

process {
    withName: COPY_SELECTED_FILE {
        cpus   = 1
        memory = '512 MB'
        time   = '2h'
    }

    withName: WRITE_MANIFEST {
        cpus   = 1
        memory = '256 MB'
        time   = '15m'
    }
}
